name: hello-world

on: push

jobs:
  my-job:
    runs-on: ubuntu-latest
    steps:
      - name: my-step
        run: echo "hello world"

  second-job:
    needs: my-job
    runs-on: ubuntu-latest
    steps:
      - name: Second-job
        run: echo "This is the second job, it runs only after the first job"
